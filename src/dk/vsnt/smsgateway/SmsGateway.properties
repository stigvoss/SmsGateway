# Listening port of service
# e.g. 8080 makes http://localhost:8080/send
Port=8080

# Location of Gammu SMSD inject
# Debian default: /usr/bin/gammu-smsd-inject
Gammu=/usr/bin/gammu-smsd-inject

# Root of API
# e.g. /sms makes http://localhost:8080/sms/send
ApiRoot=/sms